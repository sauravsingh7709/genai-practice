function addTask() { var input = document.getElementById('taskInput'); var task = input.value.trim(); if (task === '') return; var ul = document.getElementById('taskList'); var li = document.createElement('li'); li.textContent = task; var delBtn = document.createElement('button'); delBtn.textContent = 'Delete'; delBtn.className = 'delete-btn'; delBtn.onclick = function() { ul.removeChild(li); }; li.appendChild(delBtn); ul.appendChild(li); input.value = ''; input.focus(); } document.getElementById('taskInput').addEventListener('keyup', function(event) { if (event.key === 'Enter') addTask(); }); 
